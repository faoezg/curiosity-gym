To run this code, one has to install uv and then sync it.

To ensure all experiments output will be stored correctly please create the
 - heatmaps dir
 - figures dir
 - stats dir
in the dir form which you plan to execute the command below.

using `uv run python ./test_everything.py` will run all experiments
